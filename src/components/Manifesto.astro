<section class="manifesto" id="manifesto">
  <h2 class="eyebrow"><span class="section-counter" data-count="01">00</span>Manifesto</h2>
  <div class="lines">
    <p class="line">Prototypes are easy. Production is the work.</p>
    <p class="line">I build the pipeline, not just the prompt.</p>
    <p class="line">Every model ships with a plan for when it's wrong.</p>
  </div>
</section>

<style>
  .manifesto {
    position: relative;
    padding: 14rem 8vw;
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .eyebrow {
    font-family: var(--font-mono);
    color: var(--accent);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    font-size: 0.85rem;
    margin: 0;
  }

  .lines {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    max-width: 28ch;
  }

  .line {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 300;
    font-size: clamp(1.75rem, 4vw, 3rem);
    line-height: 1.2;
    margin: 0;
    color: var(--text);
  }

  .line:last-child {
    color: var(--warm);
  }

  @media (max-width: 640px) {
    .manifesto {
      padding: 8rem 6vw;
      gap: 1.5rem;
    }
  }
</style>
